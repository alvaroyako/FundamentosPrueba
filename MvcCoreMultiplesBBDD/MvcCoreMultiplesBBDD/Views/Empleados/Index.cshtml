@model IEnumerable<MvcCoreMultiplesBBDD.Models.Empleado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Oficio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdEmpleado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Oficio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salario)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                <a asp-action="Details" asp-controller="Empleados" asp-route-id="@item.IdEmpleado">Detalles </a>|
                <a asp-action="Delete" asp-controller="Empleados" asp-route-id="@item.IdEmpleado">Delete </a>|
                <a asp-action="UpdateSalario" asp-controller="Empleados" asp-route-id="@item.IdEmpleado">Salario </a>|
            </td>
        </tr>
}
    </tbody>
</table>
